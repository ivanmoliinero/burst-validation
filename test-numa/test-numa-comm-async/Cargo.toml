[package]
name = "test-numa-comm-async"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
tokio = { version = "1.52.3", features = ["rt", "sync", "macros"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2.186"
